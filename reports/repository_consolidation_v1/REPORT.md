# Repository Workspace Consolidation V1

## Outcome

The workspace was consolidated at the navigation/documentation layer while
preserving scientific lineage and evidence paths. No tracked config, report,
output, artifact, test, or source module was moved, renamed, rewritten, or
deleted. Existing historical documentation was already under `docs/_archive/`;
the performance closeout is now explicitly indexed there.

## Audit Snapshot

Generated by `scripts/repository_consolidation_audit_v1.py` from commit
`e2804c1`:

| Surface | Files | Tracked |
|---|---:|---:|
| docs | 121 | 119 |
| configs | 227 | 226 |
| scripts | 518 | 255 |
| reports | 371 | 354 |

The machine-readable inventory is `file_inventory.csv`; path references are in
`reference_edges.csv`; conservative classifications are in `classification.csv`.
Large files are inventoried by metadata without expensive hashing. No detached
script was auto-archived; `dead_code_candidates.csv` is advisory only.

## Navigation Changes

- Added `configs/README.md`, `scripts/README.md`, and `reports/INDEX.md`.
- Updated `docs/DOC_INDEX.md` and `docs/CURRENT_PIPELINE.md` with ACTIVE,
  PINNED_REFERENCE, and CLOSED boundaries.
- Added the performance program closeout under
  `docs/_archive/09_model_research_and_productivity_history/`.
- Clarified current Fast/Full baselines and intentional Phase H non-pursuit in
  both repository READMEs.

## Integrity

No evidence bytes were changed. `git diff --check`, Python compilation of the
audit tool, and the documentation index checker passed. Full pytest and Ruff
could not run in this environment because `pandera` and `ruff` are not installed;
these are environment prerequisites, not test regressions.
