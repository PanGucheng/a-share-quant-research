# D1 正式冻结

2026-09-10：评估用户随本次实施请求提供的意见后，接受 candidate_v2 的 R218/C201/H358。
D1 CLOSED / REPRESENTATIONS FROZEN / D2 NOT YET RUN。

[冻结合同](freeze_v1/freeze.json)与[正式语义补充](freeze_v1/semantics.json)承接授权；
原 recipe、计划、candidate_v1/v2 和全量回执原样保留，其旧状态是历史状态。
本文件替代旧报告中“等待人工冻结/D2未授权”的当前状态。

接受 n_min=100、family 内至少一半节点、跨 family 全部可用、共同 raw U61。
递归 U 保留 quality_limited_raw_carry_through；32 个 Alpha101 的经济解释仍未解决。
Dense 输出是冻结桶内可用 eligible lag 的平均 rank，不是固定完整 lag 路径。
接受 variable-node composition 与 range 的完整 family 覆盖代价，不作阈值扫描。
0.995 是严格同语义同尺度替代门槛，不是通用相关剪枝；当前无新增近似替代。

正式冻结通过独立 overlay 绑定旧 recipe/DAG、ordered arms、U、源代码/metadata、
168 个月回执、全量审阅、未来 evaluator 合同和 B/S learner 合同。
旧 diagnostic_policy 字段名保留以维护经验证实现；其数值现由 overlay 正式批准。

D2 仅授权固定 V3 float64 Sequence 的 2015–2023 共 27 个模型及独立 saved-model replay。
长任务仍由用户执行。尚未运行 D2；outcome evaluation、pool comparison、D3 和 2024+ 研究值均不开放。
