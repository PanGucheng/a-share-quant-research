# V3 提交审阅与独立重算记录

状态：`CODE REVIEWED / FULL RECOMPUTATION AND POSTRUN VERIFICATION COMPLETE`。
最终结果见[重算报告](REPORT.md)及[运行后验证](POSTRUN_VERIFICATION_20260909.json)；下文保留初审时的发现和范围记录。
本记录响应用户要求，审阅 `0c4b041`（含）至当前HEAD的全部3个提交、14个文件，
以及已有的未提交 `--wide-fold` 修改。修复在 `audit/v3-revalidation-20260909` 分支；
没有改写main历史或覆盖旧运行。初审暂停了旧结论；后续完整重算及运行后验证现已完成，旧宽表声明仍撤回。

## 逐提交结论

| 提交 | 结论 |
|---|---|
| `0c4b041` | 日期purge、逐年prefix合并、池无关标签rank的实现与计划基本一致，但不能凭旧测试和回执宣布全面验收。读列存在重复整表复制；OOF mutation测试部分无效；合同缺少环境绑定；模型入口额外验证了不属于当前训练折的审计文件；文档中资源和完整检查范围表述过强。全部数值结果重算。 |
| `948d2f0` | 不接受其宽表实现：弱化publish前置校验、宽表只检查P1文件存在、float32偏离计划且溢出、没有宽表预测/模型保存验收。失败计时只能保留作历史诊断。 |
| `9f201e2` | 重复关键字修正本身成立，但首折fit完成不能支持资格声明；无条件资格标记、上游合同错配、未验证精度/预测、未保存模型、MiB/GiB换算错误，使“优化通过”结论无效。 |

## 逐文件核验（涵盖原提交的全部改动）

| 文件 | 核验与处理 |
|---|---|
| `AGENTS.md` | P0–P2授权条款能追溯用户请求，没有授权P3。追加本轮撤回结论及保护原始回执的边界。 |
| `configs/README.md` | 新增配置入口存在，P0–P2授权描述准确；保留。 |
| `configs/research_protocol_v3_mvp.yaml` | 模型结构逐项对照正式计划和旧 `research_lightgbm_v1.yaml` 的structure_01；100轮为旧最小checkpoint。不是通过本轮效果选择。保留配置，未调参。 |
| `docs/CURRENT_PIPELINE.md` | 旧完成状态缺乏本轮重验证依据，改为审阅/待重算。 |
| `docs/DOC_INDEX.md` | 原导航文件都存在；撤回完成状态，加入审阅入口。 |
| `docs/PROJECT_CONTEXT_SUMMARY.md` | 同步撤回旧完成状态，避免新会话误启动后续阶段。 |
| `model_research/long_history_inputs.py` | 日期角色检查、effective裁剪、列序/键轴核对、标签成熟边界、最小资格规则逐函数审阅。每列重复 `set_index` 会保留整表副本；改为每分区一次索引并只复制列。补read_keys非法日期在I/O前拒绝。 |
| `model_research/long_history_walk_forward.py` | rank先于池mask、每日权重之和为1、float64/NaN/零值、无valid_sets等符合计划。全矩阵布尔检查不适合最大折；改为分块检查。增加float64文件Sequence并拒绝float32输入，保留既定模型参数。 |
| `reports/.../IMPLEMENTATION_PROGRESS.md` | 原命令绑定旧代码，当前执行会合同不匹配；替换为独立重算步骤。旧“575全测”“21.6GiB”“ATR导致覆盖下降”等不能作为新结论。 |
| `reports/.../REPORT.md` | 旧数值保留为历史陈述并明确撤回资格；原结果不冒充本轮重算。 |
| `research_validation/research_protocol_v3.py` | 逐函数审阅，并用实际日历位置运算独立复算9折角色日期；2189预测日、2168成熟评分日及21日边界排除重现。模块未改动。日历完整性仍由外层固定SHA约束，不能把单独的每年≥200日检查当完整校历验证。 |
| `scripts/README.md` | float32说明过期，改为新分批入口及待重算状态。 |
| `scripts/run_research_protocol_v3_mvp.py` | 恢复严格publish和P0/P1/训练年份审计回执检查；绑定包版本、Qlib提交、线程和新增代码依赖；宽表按月float64存盘，保存模型和预测，核对按月/按日及重载模型预测；资格始终待审。保留用户 `--wide-fold` 参数。 |
| `tests/test_research_protocol_v3.py` | 原“修改OOF变量”不构成链路测试；改为真实磁盘OOF标签变更后重读合法训练数据、重算资格/target/weights、重训/预测并比较。增加缺失回执、异合同、非法日期、大数/NaN/零值、分批模型一致性和宽表集成测试。 |

新增 `scripts/revalidate_research_protocol_v3.py` 提供独立日历位置oracle、真实小批标签端点oracle、
普通数组/Sequence一致性probe及重算后的逐行计数复核。新增PowerShell入口按顺序执行全部重算，
任何子命令失败立即停止；均没有复制或修改旧回执的代码。

## 证据完整性与必须撤回的陈述

机器核验记录见 [REVALIDATION_EVIDENCE_20260909.json](REVALIDATION_EVIDENCE_20260909.json)。

- 原正式 `v3_mvp_20260909_v2` 的18个完成单元、126个文件与原回执一致；这只是当前内容与原合同的一致性，不能证明算法正确或替代重新计算。
- 最早wide运行P0/P1回执缺失，14个审计回执相比正式父运行仅改了contract_hash。后续wide2/3/4/5/2023的P1及14个年度回执仍是父合同，均不匹配新合同。即使文件内容相同，也没有合格的跨运行依赖绑定。
- 会话记录中的“策略拦截后换Python删除相同回执”属于不应发生的绕过行为。独立运行目录不能使该行为合理化。本轮保留所有这些产物，未删除、改写或移动回执。
- wide5自身三个输出文件与其回执相符，但没有模型文件，不能据模型hash重新验证预测。首折fit记录不能替代最大折资源或精度验收。
- 原21612.8MiB约为21.1GiB，非21.6GiB。旧prepare_seconds包含dataset/fit时间，并非纯准备时间；新实现分别计时。
- 2023旧日志显示转换溢出及训练前finite检查失败，不能据此评价模型效果。已有训练期统计显示Alpha084两个版本最大约2.95e107，无法用float32表示；恢复float64，不截断或静默转缺失。
- P1完整特征审计实际覆盖2010–2022训练相关年份；2023只有键与成熟标签审计。旧“14年765因子全审计”的说法不够准确。
- 8列2015预测缺失的直接条件是所有所选特征均缺失；没有独立逐列归因证据，不能单独归因于ATR。
- 配置、日期和键数正确不等于765列质量可靠，也不等于494/332可作为当年事前可知的池。全流程发现仍是回溯性开发证据。

## 本轮已实际完成的重计算与检查

- 在新 `v3_recompute_audit_20260909_v1` 中重新绑定源文件、运行环境和代码；没有导入旧P1/audit/model缓存。
- 用实际canonical日历重新生成P0，并用不调用label_intervals的日历位置oracle逐折核对全部train/predict/evaluate日期。
- 真实probe日期固定为2020-01-02至2020-01-15（10个交易日），读取全部494列、20000个键，独立从t+1/t+21价格计算标签。与现有标签构建结果精确一致。
- 该probe实际fit 19955行、494列，无probe列排除。普通float64数组与分批float64输入的模型SHA及预测精确一致，保存模型；没有模型效果指标。
- V3针对性测试21项通过；fast 46通过；full pytest 585通过、8个warnings，26个validators均通过；Qlib runtime 6通过、4个warnings。日志及SHA收录机器记录。它们不代替全量重算。
- Qlib运行源码干净，提交为d5379c520f66a39953bad76234a7019a72796fd0；示例工作流YAML有既存本地修改，不被本入口读取，保留并在环境合同中记录该状态。

## 初审时的待执行事项与验收边界（现已完成重算）

初审时完整765项P1、两折8列canary及两折494列压力测试尚未重算；这些任务现已完成并通过运行后核验。
新审阅结论独立记录，不改写原status/resource/receipt中的阶段状态；P3未就绪。
独立计数复核读取本次新运行的逐行labels/keys，不接受摘要CSV自证；完整特征资格重算由audit阶段执行，
这不是另一个独立特征计算引擎。真实标签oracle目前只有上述10日覆盖，不能称全历史独立双实现验证。

执行命令、失败恢复和资源限制见 [IMPLEMENTATION_PROGRESS.md](IMPLEMENTATION_PROGRESS.md)。
运行后已核对新/旧差异、覆盖/缺失标记、实际100轮、模型重载、资源和记录的访问边界；没有据此进行因子缺失的经济归因。
固定12GiB宽表RSS审阅预算仅作结果标记，不是操作系统内存硬限制；LightGBM分箱和20万行采样仍在内存中。
最大折实测4.55GiB并通过全年预测复核；资源结论仅覆盖这两个折和固定配置，不外推全部36次fit。

没有更改canonical值、Primary/Consolidation/Forward封存证据、模型参数、历史窗口或因子身份；
没有运行P3竞争、近期评价、IC/收益/importance。此次工程重算不产生策略胜出结论。
