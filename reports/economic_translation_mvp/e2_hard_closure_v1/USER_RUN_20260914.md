# 2026-09-14 用户执行完成/中断复核

用户依次启动四阶段，States 报错后后续粘贴命令仍启动了 Dividends，用户手动停止。`quotes 4276 / 4416` 是进度行，不是错误。当前未发现这几个用户扫描仍在运行。

已逐完成块核对全部文件 SHA256，结果见 [USER_RUN_20260914.json](USER_RUN_20260914.json)：

| 阶段 | 已完成 | 行数 | 结论 |
|---|---:|---:|---|
| Quotes | 4,416 / 4,416 | 7,429,962 | complete.json 与全部 17,664 个输出文件 hash 一致；不要重跑 |
| States | 118 / 4,416 | 245,439 | 下一个 SH600143 留有 RuntimeError 失败 receipt；118 个数据文件 hash 通过 |
| Dividends | 5 / 2,189 日期 | 2 | 2015-01-05 至 01-09 已保存，其后用户停止；不是无分红全年证明 |

States 每块一个 parquet，Quotes 每块四个被绑定文件；receipt 单独保留。计数以 JSON 的 checked_files 为准。

原采集脚本把源 status 丢弃，只记通用 RuntimeError，无法还原用户那次的具体错误。一次只请求 SH600143、2015-01-05 至 2023-12-29 的独立诊断返回 BaoStock **10002007：网络接收错误**，0 行；该零行不是无数据证据。不能证明用户原错误必定相同，但当前确实有源通信故障。失败诊断目录单独保留，未覆盖原记录。

新增 resume_e2_hard_history 只恢复 States / Dividends，不支持重跑 Quotes。逐块校验并引用父目录中完成的数据，不复制或改写父目录。新尝试写 recovery_states_v1 / recovery_dividends_v1；每次手动重新运行以新的 attempt 目录保存失败或中断记录，成功块继续复用。源 status 得以保留，失败立即退出，不自动跳过、不换来源、不变日期范围。

父 scope 与已运行代码 hash 必须仍匹配；新增恢复代码单独绑定，不通过改写旧 receipt 兼容新代码。Bao 失败连接关闭后才可重新登录；没有修改 vendor 库。socket 超时设为 20 秒，这不是整体作业时限。恢复阶段很长，仍由用户执行，Codex 没有启动完整恢复。

新增 synthetic 恢复验证覆盖父完成块复用、父失败记录不改写、网络失败/KeyboardInterrupt 后新尝试、损坏 hash 阻止网络调用、2024 响应拒绝和分红日期续跑。最终六项及 Ruff 均已通过。原 61 项执行链、fast 46 项与 Qlib runtime 6 项结果保留。

Quotes 完成仅说明扫描输出完整，单位异常、状态/事件/身份闭环仍待核验。**E2 STILL BLOCKED；不得进入 E3/E4 或真实账户回测。**

现在只执行 [RUNBOOK](RUNBOOK.md) 中的 States 续跑命令；成功后再执行 Dividends。不要把多个阶段一次性粘贴进交互式 PowerShell。
